IOT程序结构说明：
1、iot.c
	主程序，负责读取配置文件、获取sensor数据并形成json格式、发送数据至mqtt。
	支持前台、后台两种运行方式。

2、iot.cfg
	配置文件。主要配置内容包括：mqtt服务器连接参数、程序运行模式（前台、后台）等。

3、iot.h
	头文件主要用来定义全程变量，通常变量名称与iot.cfg里的变量名称相同

4、yl.c及ll.c：sensor子程序
	各种传感器的读取子程序
5、Makfile文件
	自动创建iot可执行文件

